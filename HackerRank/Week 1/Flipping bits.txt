#include <stdio.h>
unsigned int FlippingBits(unsigned int n) {
    return n ^ 0xFFFFFFFF;
}
int main() {
    int a;
    scanf("%d", &a);
    for (int i = 0; i < a; i++) {
        unsigned int n;
        scanf("%u", &n);
        printf("%u\n", FlippingBits(n));
    }
    return 0;
}
